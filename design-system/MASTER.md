# Harbor Review

Skill search: ui-ux-pro-max, `operations dashboard data dense`. Verified fit: Data-Dense Dashboard, enterprise operational data, accessible status labels and filters. The returned marketing/Enterprise Gateway page pattern is not applicable; use PRD 2's three-screen structure instead. Stack: React, explicitly selected by user.

Use the current apricot/terracotta workspace with dark brown text. Palette: canvas #F4DDC8, panels #FCEBDC, primary text #352419, secondary text #70523E, primary action #A04420 with white text. Success, uncertainty, discrepancy and failure retain labeled semantic colors. Status includes text; never rely on color. System sans-serif keeps preview independent of font providers; monospace for IDs and tabular numbers. Main body 16px; metadata minimum 12px. Spacing 4/8/12/16/24/32px; borders 1px; corners 10–14px.

Desktop: 224px navigation rail, max-width content, metric cards and readable comparison rows. Mobile: compact top navigation, single-column cards, labeled SI/BL values. Do not hide essential case status fields to fit. Controls at least 44px high, explicit labels, visible 3px focus outline, native semantic buttons/links/details. Use restrained panel depth and short row entrance/hover motion, disabled by reduced-motion preferences. Never invent pipeline activity. Respect reduced motion. Hover only enhances click/focus behavior.

## Current interaction conventions

The primary user is a shipping documentation coordinator checking a draft BL against SI. Keep the overview comprehensive with search/category/review-state filters. Prioritize unresolved fields and human-applied values in the case workspace; routine matches may collapse but must retain complete evidence. Recorded progress must distinguish human input, external action, failure and completion; completion is not proof that no correction remains.

Telegram uses short, conversational action-first messages. Keep side/field/unit, synthetic labels, known mismatch and consequential action meaning visible. Evidence, provenance and run IDs live behind View details. Show exact previews, explicit confirm/cancel and ungrounded override warnings. Never imply an accepted decision has finished processing. Local dashboard instructions must not promise phone access.
