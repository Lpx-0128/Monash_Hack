# Server only; do not put secrets in VITE_* variables.
PORT=5173
HOST=127.0.0.1
# Set true when hosted behind HTTPS.
COOKIE_SECURE=false
# Build-time future integration settings. Default is synthetic mode.
# VITE_API_MODE=live
# VITE_API_BASE=/api/v1
