# Set HARBOR_DOMAIN in the caddy systemd environment before loading.
{$HARBOR_DOMAIN} {
    encode zstd gzip
    header {
        Referrer-Policy no-referrer
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
    }
    reverse_proxy 127.0.0.1:5173
}
